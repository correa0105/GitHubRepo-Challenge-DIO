import styled from "styled-components";

export const ItemContainer = styled.div`
    width: 80%;
    
    h3 {
        font-size: 3.5rem;
        color: #FAFAFA;
    }

    p {
        font-size: 1.9rem;
        color: #FAFAFA60;
        margin-bottom: 1.6rem;
    }

    a.repositorio {
        font-size: 1.6rem;
        color: #FFFFFF;
    }

    a.remover {
        font-size: 1.6rem;
        color: #FF0000;
    }

    hr {
        color: #FAFAFA60;
        margin: 2rem 0;
    }
`